when:
  - event: push
    branch: deploy

steps:
  - name: build
    image: debian
    commands:
      - echo "This is the build step"
      - echo "binary-data-123" > executable
  - name: a-test-step
    image: node:22
    commands:
      - echo "Building ..."
      - npm i
      - npm run build --if-present
